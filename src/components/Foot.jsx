import React from 'react';
import { Alert } from 'reactstrap';

const Foot = (props) => {
    return (
        <div>
            <Alert color="primary">
                Thank you for visiting the site!
            </Alert>
        </div>
    );
}

export default Foot;